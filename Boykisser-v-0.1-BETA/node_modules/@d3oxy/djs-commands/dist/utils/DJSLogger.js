"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const chalk_1 = __importDefault(require("chalk"));
class DJSLogger {
    constructor() {
    }
    /**
    * Logs a success message to the console.
    *
    * @param {string} message - The message to log.
    * @param {boolean} [exit=false] - Whether to exit the process after logging the message.
    */
    async success(message, exit = false) {
        const { log } = console;
        const prefix = chalk_1.default.cyan(chalk_1.default.bold("DJSCommands >>"));
        log(`${prefix} ${chalk_1.default.green(message)}`);
    }
    /**
    * Logs a info message to the console.
    *
    * @param {string} message - The message to log.
    * @param {boolean} [exit=false] - Whether to exit the process after logging the message.
    */
    async info(message, exit = false) {
        const { log } = console;
        const prefix = chalk_1.default.cyan(chalk_1.default.bold("DJSCommands >>"));
        log(`${prefix} ${chalk_1.default.blue(message)}`);
    }
    /**
    * Logs a warn message to the console.
    *
    * @param {string} message - The message to log.
    * @param {boolean} [exit=false] - Whether to exit the process after logging the message.
    */
    async warn(message, exit = false) {
        const { log } = console;
        const prefix = chalk_1.default.cyan(chalk_1.default.bold("DJSCommands >>"));
        log(`${prefix} ${chalk_1.default.yellow(message)}`);
    }
    /**
    * Logs a error message to the console.
    *
    * @param {string} message - The message to log.
    * @param {boolean} [exit=true] - Whether to exit the process after logging the message.
    */
    async error(message, exit = true) {
        const { log } = console;
        const prefix = chalk_1.default.cyan(chalk_1.default.bold("DJSCommands >>"));
        log(`${prefix} ${chalk_1.default.red(message)}`);
        if (exit)
            process.exit(1);
    }
}
exports.default = DJSLogger;
